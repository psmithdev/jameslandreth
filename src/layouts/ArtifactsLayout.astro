---
import '../styles/global.css';
import { isAdmin } from '../lib/auth';

interface Props {
  title?: string;
}

const { title = 'Family Treasures' } = Astro.props;
const user = (Astro.locals as any).user;
const canManageArtifacts = isAdmin(user);
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <title>{title}</title>
</head>
<body class="min-h-screen bg-parchment text-ink antialiased">

  <!-- Top accent strip -->
  <div class="h-[3px] bg-stamp-art"></div>

  <header class="bg-parchment border-b border-rule">
    <div class="max-w-6xl mx-auto px-6 py-5">
      <div class="flex items-end justify-between gap-4">
        <a href="/" class="no-underline group">
          <p class="font-mono text-[11px] sm:text-[13px] tracking-[0.18em] uppercase text-stamp-art mb-1.5">Heritage Collection</p>
          <h1 class="font-display text-2xl sm:text-3xl text-ink leading-none group-hover:text-stamp-art transition-colors duration-200">Family Treasures</h1>
        </a>

        <!-- Desktop nav -->
        <nav class="hidden md:flex items-center gap-8 pb-0.5">
          <a href="/" class="font-mono text-[14px] tracking-[0.12em] uppercase text-ink-muted hover:text-stamp-art transition-colors">Browse Items</a>
          <a href="https://jameslandreth.com" class="font-mono text-[14px] tracking-[0.12em] uppercase text-ink-muted hover:text-stamp-art transition-colors" target="_blank">jameslandreth.com</a>
          {user ? (
            <div class="flex items-center gap-6">
              {canManageArtifacts && (
                <a href="/admin" class="font-mono text-[13px] tracking-[0.12em] uppercase text-stamp border border-stamp px-3 py-1.5 hover:bg-stamp hover:text-card transition-colors">Upload</a>
              )}
              <span class="font-mono text-[13px] text-rule">{user.display_name || user.email}</span>
              <a href="/api/auth/logout" class="font-mono text-[14px] tracking-[0.12em] uppercase text-ink-muted hover:text-stamp transition-colors">Logout</a>
            </div>
          ) : (
            <a href="/login" class="font-mono text-[13px] tracking-[0.12em] uppercase text-card bg-stamp-art hover:bg-stamp-art-hover px-4 py-2 transition-colors">Login</a>
          )}
        </nav>

        <!-- Mobile menu toggle -->
        <button
          id="mobile-menu-toggle"
          type="button"
          class="md:hidden inline-flex items-center justify-center border border-rule p-2 text-ink-muted transition-colors hover:border-stamp-art hover:text-stamp-art"
          aria-label="Open menu"
          aria-expanded="false"
          aria-controls="mobile-menu"
        >
          <svg data-menu-open class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8" d="M4 7h16M4 12h16M4 17h16" />
          </svg>
          <svg data-menu-close class="hidden h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8" d="M6 6l12 12M18 6L6 18" />
          </svg>
        </button>
      </div>

      <!-- Mobile menu -->
      <nav id="mobile-menu" class="hidden md:hidden mt-4 flex-col border-t border-rule pt-2">
        <a href="/" class="block py-3 font-mono text-[14px] tracking-[0.12em] uppercase text-ink-muted transition-colors hover:text-stamp-art">Browse Items</a>
        <a href="https://jameslandreth.com" target="_blank" class="block border-t border-rule/60 py-3 font-mono text-[14px] tracking-[0.12em] uppercase text-ink-muted transition-colors hover:text-stamp-art">jameslandreth.com</a>
        {user ? (
          <div class="mt-2 flex flex-col gap-3 border-t border-rule pt-3">
            {canManageArtifacts && (
              <a href="/admin" class="inline-block self-start border border-stamp px-3 py-1.5 font-mono text-[13px] tracking-[0.12em] uppercase text-stamp transition-colors hover:bg-stamp hover:text-card">Upload</a>
            )}
            <div class="flex items-center justify-between gap-4">
              <span class="font-mono text-[13px] text-rule">{user.display_name || user.email}</span>
              <a href="/api/auth/logout" class="font-mono text-[14px] tracking-[0.12em] uppercase text-ink-muted transition-colors hover:text-stamp">Logout</a>
            </div>
          </div>
        ) : (
          <a href="/login" class="mt-3 inline-block bg-stamp-art px-4 py-2.5 font-mono text-[13px] tracking-[0.12em] uppercase text-card transition-colors hover:bg-stamp-art-hover">Login</a>
        )}
      </nav>
    </div>
  </header>

  <main>
    <slot />
  </main>

  <footer class="border-t border-rule mt-20">
    <div class="max-w-6xl mx-auto px-6 py-6 flex items-center justify-between">
      <p class="font-mono text-[13px] tracking-[0.15em] uppercase text-rule">Walsh · Flick · Littlefield · Litzel Heritage Collection</p>
      <p class="font-mono text-[13px] tracking-[0.12em] uppercase text-rule">
        Contact <a href="mailto:aledaandjim@yahoo.com" class="text-stamp-art hover:text-stamp-art-hover transition-colors no-underline">aledaandjim@yahoo.com</a>
      </p>
    </div>
  </footer>

  <script>
    const menuToggle = document.getElementById('mobile-menu-toggle');
    const mobileMenu = document.getElementById('mobile-menu');
    const openIcon = menuToggle?.querySelector('[data-menu-open]');
    const closeIcon = menuToggle?.querySelector('[data-menu-close]');

    function setMenu(open: boolean) {
      if (!menuToggle || !mobileMenu) return;
      mobileMenu.classList.toggle('hidden', !open);
      mobileMenu.classList.toggle('flex', open);
      openIcon?.classList.toggle('hidden', open);
      closeIcon?.classList.toggle('hidden', !open);
      menuToggle.setAttribute('aria-expanded', String(open));
      menuToggle.setAttribute('aria-label', open ? 'Close menu' : 'Open menu');
    }

    menuToggle?.addEventListener('click', () => {
      setMenu(mobileMenu?.classList.contains('hidden') ?? true);
    });

    // Collapse the menu when resizing up to the desktop breakpoint.
    window.matchMedia('(min-width: 768px)').addEventListener('change', (e) => {
      if (e.matches) setMenu(false);
    });
  </script>

</body>
</html>
